name: Deploy MultiDocker
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t marc89763/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true marc89763/react-test npm test

      - run: docker build -t marc89763/multi-client ./client
      - run: docker build -t marc89763/multi-nginx ./nginx
      - run: docker build -t marc89763/multi-server ./server
      - run: docker build -t marc89763/multi-worker ./worker

      - run: docker push marc89763/multi-client
      - run: docker push marc89763/multi-nginx
      - run: docker push marc89763/multi-server
      - run: docker push marc89763/multi-worker
